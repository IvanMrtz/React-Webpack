{
  "presets": [
    [
      "@babel/preset-env",
      {
        "corejs": 3.14,
        "useBuiltIns": "usage"
      }
    ],
    [
      "@babel/preset-react",
      {
        "runtime": "automatic" //This allow no need to import React in every file
      }
    ]
  ]
}
